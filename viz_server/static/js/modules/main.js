/**
 * Main entry point for the 3D visualization application
 */

// Initialize the application when the DOM is ready
document.addEventListener('DOMContentLoaded', function () {
    // Make sure to call the globally available initializeApplication function
    initializeApplication();
});
